import React from 'react';
// import { useParams } from 'react-router-dom';

const Video = () => {

    // let { id } = useParams();

    // console.log(id);

    return (
        <div>
            toto
        </div>
    );
};

export default Video;

